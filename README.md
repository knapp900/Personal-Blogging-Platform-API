#Personal Blogging Platform API is an education project

## PostgeSQL

```shell
docker run --name blog-db -p 5432:5432 -e POSTGRES_USER=blog -e POSTGRES_PASSWORD=password -e POSTGRES_DB=blog postgres:16
```
